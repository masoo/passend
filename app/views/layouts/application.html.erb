<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="cocuu is setcions creation services.">
  <title>Passend</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= stylesheet_link_tag '//maxcdn.bootstrapcdn.com/bootswatch/3.2.0/cosmo/bootstrap.min.css' %>
  <%= javascript_include_tag '//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js' %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container-fluid">
  <nav class="navbar navbar-default" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">Passend</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active">
          <%= link_to messages_path do %>
            <span class="glyphicon glyphicon-inbox"></span> Inbox
          <% end %>
        </li>
        <li>
          <%= link_to messages_path do %>
            <span class="glyphicon glyphicon-upload"></span> Sended
          <% end %>
        </li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <span class="glyphicon glyphicon-user"></span> <%= current_user.email %>
            <b class="caret"></b>
          </a>
          <ul class="dropdown-menu">
            <li>
              <%= link_to edit_current_user_path do %>
              <span class="glyphicon glyphicon-cog"></span> Settings
              <% end %>
            </li>
            <li class="divider"></li>
            <li>
              <%= link_to current_user_session_path, :method => :delete do %>
              <span class="glyphicon glyphicon-off"></span> Sign out
              <% end %>
            </li>
          </ul>
        </li>
      </ul>
    </div>
  </nav>

  <% if notice.present? or alert.present? %>
  <div class="row">
    <% if notice.present? %>
    <div class="col-md-10 col-md-offset-1">
      <div class="alert alert-info"><p><%= notice %></p></div>
    </div>
    <% end %>
    <% if alert.present? %>
    <div class="col-md-10 col-md-offset-1">
      <div class="alert alert-danger"><p><%= alert %></p></div>
    </div>
    <% end %>
  </div>
  <% end %>

<%= yield %>

</div>
</body>
</html>
